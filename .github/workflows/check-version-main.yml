name: 'Check Version'
on:
  pull_request:
    branches:
      - release/**
jobs:
  check-version:
    runs-on: ubuntu-latest
    steps:
      - uses: check-version
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
